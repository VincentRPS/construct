construct std::vectools::nvec;

/) assigns and constructs a variable
var:<str>:hello_world = "Hello, World!";

// print that variable
// Hello, World!
print!(hello_world);

// can also be done with vectors

var:<Vec<str>>:hello_world_vec = nvec!(hello_world):

// ["Hello, World!"]
print!(hello_world_vex);
