// In construct, we provide a set of math tools and operations

fn multiply_two<Number>(number: Number) {
    return number * 2;
}

# you can also specify the bits of the Number
fn divide_four<Number>(number: Number<b32>) {
    return number / 2;
}

// and so on...

